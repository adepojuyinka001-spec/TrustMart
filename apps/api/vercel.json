{
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @trustmart/api exec prisma generate && pnpm --filter @trustmart/api build",
  "installCommand": "echo skip",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
