# Dependencies
node_modules/
.pnpm-store/

# Env / secrets — never commit production secrets (see CLAUDE.md)
.env
.env.*
!.env.example

# Build output
dist/
build/
.next/
out/
.turbo/
coverage/
*.tsbuildinfo

# Prisma
*.db
*.db-journal

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Vercel CLI project-link metadata (created by `vercel link`/`vercel dev`)
.vercel

# Docker volumes (if any local bind mounts are added)
docker-data/

# User-uploaded listing photos (local-disk MediaStorage dev implementation) — never commit
# user content; production uses a real object storage provider once one is chosen (Open
# Decision, CLAUDE.md SS39).
apps/api/uploads/
