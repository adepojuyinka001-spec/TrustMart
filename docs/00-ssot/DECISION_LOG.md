# TrustMart Decision Log

Append-only. Each entry is a decision that has actually been approved, with date and reasoning. Do not edit or delete past entries — add a new entry if a decision changes.

## 2026-08-22 — Repository and dev-stack setup
- **Decision:** Build TrustMart directly via Claude Code (CLI session) rather than requiring VS Code; VS Code remains optional.
- **Decision:** Private GitHub repository `adepojuyinka001-spec/TrustMart` is the TrustMart-owned repo of record.
- **Decision:** Local Postgres via Docker for development (not a hosted free tier like Neon/Supabase), to keep dev data local and avoid third-party exposure during early build-out.
- **Decision:** n8n will be self-hosted via Docker rather than n8n Cloud, to stay on free tooling.
- **Decision:** First scaffolding pass creates only docs + repo structure; monorepo/code skeleton and Shared Core implementation are separate, explicitly approved follow-up phases.
- **Reasoning:** Matches `CLAUDE.md` §13 preferred stack and the founder's instruction to use free tooling wherever possible.

## 2026-08-22 — Escrow tagline vs. existing SSOT campaign line
- **Conflict found:** The approved TrustMart Escrow logo lockup carries the tagline "We Hold. You Trust. We Deliver.", which was not previously documented anywhere in CLAUDE.md or the SSOT. The SSOT already documented a separate Escrow campaign/CTA line, "Secure the Deal."
- **Decision:** Both are retained as distinct, non-conflicting assets: "Secure the Deal" is Escrow's short campaign/CTA copy (e.g. button and ad copy); "We Hold. You Trust. We Deliver." is the Escrow logo lockup tagline (brand identity material, e.g. letterhead, signage, app splash). Neither replaces the master brand line, "Securing Transactions. Building Trust."
- **Reasoning:** Founder approved treating the logo tagline the same way the existing Marketplace campaign line is treated — additive, not a replacement — per `CLAUDE.md` §3.
- Recorded in `CLAUDE.md` §3 and `docs/00-ssot/TRUSTMART_SSOT_v2.0.md` §14–15.

## 2026-08-22 — Monorepo skeleton + Shared Core, first implementation pass
- **Decision:** Built the approved Phase 1 plan (pnpm/Turborepo monorepo, `apps/api` NestJS backend, `apps/web` Next.js skeleton, Prisma schema, Shared Core modules — identity/auth, users, business, RBAC, platform configuration, audit, consent, verification).
- **Decision:** Password hashing uses `bcryptjs` (pure JS) instead of native `bcrypt`, to avoid native build-tool requirements on this Windows dev machine. Free, no functional downside for this scale.
- **Decision:** `pnpm` is invoked via `npx pnpm@latest ...` rather than a global install, because `corepack enable` failed with an `EPERM` writing to `C:\Program Files\nodejs\` (no admin rights in this shell). This is a local workaround, not a project convention — a properly elevated machine can `corepack enable` normally.
- **Verified:** Both apps type-check and build cleanly; 7/7 DB-independent unit tests pass (RBAC guard positive/negative, auth service positive/negative); the Next.js placeholder page was checked in-browser and renders the correct brand colors, Montserrat font, and copy.
- **Blocked/pending:** Docker Desktop is not installed on this machine, so Postgres/n8n haven't been started — `prisma migrate dev`, the seed script, and the full e2e suite are written but not yet run. Tracked as Open Decision #6.
- **Reasoning:** Matches the approved plan; deviations (bcryptjs, npx-invoked pnpm) were pragmatic, non-architectural choices to keep the build free and unblocked on this specific machine.

## 2026-09-07 — Adopt Master Project Document v5.0 and CLAUDE.md v5.0 as authoritative
- **Decision:** Founder supplied `TrustMart_Comprehensive_Master_Project_Document_v5.0_Claude_Code.pdf` and `TrustMart_CLAUDE_v5.0.md`, and explicitly approved ("Yes, go ahead") adopting both as authoritative over the prior v2.0 documents.
- **Action taken:** `CLAUDE.md` v2.0 moved to `docs/99-superseded/CLAUDE_v2.0.md`; root `CLAUDE.md` replaced with v5.0 content (with repo-specific path references updated, and one addendum — see below). `docs/00-ssot/TRUSTMART_SSOT_v2.0.md` moved to `docs/99-superseded/TRUSTMART_SSOT_v2.0.md`. The v5.0 Master Project Document PDF was copied into `docs/00-ssot/` and now occupies the SSOT's former role in the authority order. `MASTER_DOCUMENT_REGISTER.md` updated accordingly.
- **Decision — brand tagline:** The Escrow-only logo lockup tagline "We Hold. You Trust. We Deliver." (previously an approved additive asset per the 2026-08-22 entry) is now **retired** except for historical reference, per v5.0 §5. The single official short tagline going forward is **"Find. Secure. Transact."** The corporate promise "Securing Transactions. Building Trust." is unchanged. This supersedes the 2026-08-22 "Escrow tagline vs. existing SSOT campaign line" entry.
- **Decision — repository layout deviation retained:** v5.0 §29's preferred tree uses `services/api/...` and a separate `apps/admin/`. This repository already has a working, tested `apps/api/...` NestJS backend and no separate admin app yet. Per founder approval, the deviation is recorded rather than reconciled by renaming/moving code — see the addendum in `CLAUDE.md` §29 ("Recorded deviation — this repository"). A future dedicated task may revisit this if there's a concrete reason to.
- **Not yet reconciled:** `docs/02-architecture/TRUSTMART_MARKETPLACE_TECHNICAL_ARCHITECTURE_AND_DATABASE_BLUEPRINT_v1.0.md` and `docs/09-prompts/CLAUDE_CODE_MASTER_PROMPTS.md` still reference v2.0-era wording in places. They remain "Current" for now (flagged in the Master Document Register) since no conflict has been identified yet; v5.0 wins if one surfaces.
- **Reasoning:** Founder-supplied, founder-approved update to the single source of truth. No code, schema, or business-rule behavior was changed by this entry — documentation/authority reconciliation only.

## 2026-09-07 — Portable PostgreSQL (no Docker) resolves Open Decision #6
- **Decision:** Founder chose to attempt a Docker Desktop install first; when that needed admin elevation this session doesn't have, founder approved a self-serve alternative instead: a portable (no-installer) PostgreSQL 18.6 Windows binary distribution, run as a plain background process — not a Windows service, no admin rights, no Docker.
- **Setup:** Binaries downloaded from EnterpriseDB (`https://www.enterprisedb.com/download-postgresql-binaries`) and extracted to `C:\Users\Yinka Adepoju\pgsql-portable\pgsql` (outside the git repo, not tracked). Data directory initialized at `C:\Users\Yinka Adepoju\pgsql-portable\pgdata`. Server runs on **port 5433** (not 5432, to avoid clashing with the still-documented Docker Compose option) via `pg_ctl start`, logging to `C:\Users\Yinka Adepoju\pgsql-portable\pg.log`. Database `trustmart_dev`, user `trustmart` — same credentials as `.env.example`/`docker-compose.yml` for consistency, only the port differs.
- **Blocker hit and resolved:** `initdb` initially crashed (`0xC0000005` access violation) inside `postgres.exe`. Windows Application Event Log identified the fault as occurring in `C:\WINDOWS\SYSTEM32\MSVCP140.dll` (an outdated Visual C++ 2015-2022 Redistributable). Founder installed the current redistributable (`winget install --id Microsoft.VCRedist.2015+.x64 -e --source winget`, run elevated), which fixed it — `initdb` then succeeded on retry with no other changes.
- **`apps/api/.env` created** (gitignored, not committed) pointing `DATABASE_URL` at `postgresql://trustmart:trustmart_dev@localhost:5433/trustmart_dev?schema=public`, plus dev-only JWT secrets.
- **`apps/api/package.json`:** added the `"prisma": { "seed": "ts-node prisma/seed.ts" }` config block required by `prisma db seed` (script existed, but the seed-config block Prisma actually reads was missing).
- **`apps/api/test/jest-e2e.json`:** added `"testTimeout": 30000` — the default 5000ms Jest hook timeout was too tight for a cold `AppModule` compile + first DB connection; this is a test-infra fix, not a change to test behavior/assertions.
- **Verified:** `prisma migrate dev --name init` applied cleanly; `prisma db seed` completed (roles/permissions/default platform configuration); all 24 DB-independent unit tests pass; all 4 e2e tests pass against the live database, including the RBAC positive/negative case and the audit-log assertion.
- **Open Decision #6 is now resolved** — move it from `OPEN_DECISIONS.md` to this log (done below) and update `IMPLEMENTATION_STATUS.md`.
- **n8n:** still not running (no dependency on it yet for this phase). It can run later via `npx n8n` (no Docker needed) or Docker if the founder installs Docker Desktop for other reasons — not urgent.
- **Reasoning:** Unblocks Shared Core verification and all downstream Marketplace/Escrow work without requiring admin-elevated, heavier tooling (Docker Desktop + WSL2) on this machine. Non-architectural, dev-environment-only decision — production database hosting remains a separate, still-open decision (Open Decision #3).

## 2026-09-07 — Wired Marketplace Core (Category/Listing/BuyerRequest/Matching) into AppModule; built the Matching orchestration layer
- **Context:** Founder asked to continue autonomously through the implementation phases without waiting for per-step approval, while still stopping for anything on CLAUDE.md's own Stop Conditions list (legal/funds structure, provider selection, security weakening, confidential-rate exposure, destructive migrations, etc.). This entry and the next few implementation steps proceed under that standing instruction.
- **Finding:** `CategoryModule`, `ListingModule`, `BuyerRequestModule` had services/controllers/DTOs written (Phase 2) but were never imported into `AppModule` — none of their endpoints were actually reachable. Additionally, both `ListingService` and `BuyerRequestService` already imported and called `MatchingEngineService` from `../matching/matching-engine.service`, but that file did not exist — only the pure `matching-engine.util.ts` scorer (Phase 3 groundwork) was present. The app would not have compiled if these modules had been wired in as-is.
- **Built:** `MatchingEngineService` (orchestration: loads active listings/buyer-requests for a subcategory, builds `evaluateMatch()` input from stored `MatchingProfile`/`MatchingCriterion` weights and buyer requirement overrides, upserts `Match`/`MatchCriterionResult`, applies the configurable threshold, classifies score bands, emits `marketplace.match.created` via `@nestjs/event-emitter` only on newly-qualifying pairs), `MatchingProfileService` (versioned admin-managed weight sets — creating a new version deactivates the previous one), `MatchingController` (admin profile management gated on `matching:manage`; buyer/seller match views, ownership-scoped, seller view excludes buyer budget/requirement data per CLAUDE.md SS13).
- **Registered** `CategoryModule`, `ListingModule`, `BuyerRequestModule`, `MatchingModule`, and `EventEmitterModule.forRoot()` in `AppModule` (the `@nestjs/event-emitter` package was a declared dependency but not actually installed — `pnpm install` via `npx pnpm@9` fixed this; `npx pnpm@latest` resolved to an unstable 12.3.4 that failed with an internal path error, so pinning to `@9` — matching the lockfile's `lockfileVersion: '9.0'` — is now the working invocation on this machine).
- **Bug found and fixed:** Prisma `BigInt` fields (money/budget) can't be JSON-serialized by Express. A `toJSON` polyfill existed in `main.ts`'s `bootstrap()` but never ran for e2e tests (which construct `AppModule` directly, bypassing `main.ts`) or any other non-`main.ts` entry point. Moved it to `src/bigint-json.polyfill.ts`, imported for its side effect at the top of `app.module.ts`. This was a real, previously-undiscovered defect — it just never surfaced because the affected modules were never reachable before this pass.
- **Seed data extended:** `category:manage`, `listing:moderate`, `matching:manage` permissions were referenced by existing `@RequirePermission(...)` guards but never seeded — meaning ADMIN would have silently failed every one of those checks. Added them, plus `marketplace.match_budget_weight_percent` / `marketplace.match_location_weight_percent` (25% each, matching the SSOT's illustrative Real Estate weight example — an initial default, not a fixed rule).
- **Verified:** `nest build` clean; 24/24 unit tests pass; 7/7 e2e tests pass (4 pre-existing Shared Core + 3 new `test/matching.e2e-spec.ts` covering the full category→subcategory→attribute→listing→buyer-request→match flow, hard-requirement disqualification, threshold qualification, seller-facing privacy, and RBAC on category/matching-profile management).
- **Reasoning:** Completes the wiring gap left from Phase 2 and delivers Phase 3's core (deterministic Matching). No business rule, fee, financial, or security-authorization behavior was invented — thresholds/weights remain `PlatformConfiguration`-driven per CLAUDE.md SS4/SS9, and the bug fixes restore documented intent (BigInt-safe money, RBAC permissions that were already assumed to exist) rather than introducing new behavior.

## 2026-09-07 — Built Interest / Lead / Contact Access (rest of Phase 3)
- **Context:** Continuation of the same standing "proceed autonomously, stop only on CLAUDE.md's own Stop Conditions" instruction as the previous entry.
- **Built:** New `EngagementModule` (`apps/api/src/modules/engagement/`) covering:
  - `InterestService` — "I'm Interested" (CLAUDE.md SS11) creates an `Interest` and its `Lead` atomically in one transaction; rejects self-interest and duplicate interest on the same listing; optional `matchId` is verified to actually belong to the requesting buyer and listing.
  - `LeadService` — seller-driven status transitions through the full flow (NEW→VIEWED→CONTACTED→INSPECTION_SCHEDULED→NEGOTIATING→TRANSACTION_STARTED→WON/LOST); every transition is recorded as a `LeadActivity`; WON/LOST/SPAM_FRAUD are terminal (no further transition accepted).
  - `ContactAccessService` — enforces the buyer-contact-privacy rule (CLAUDE.md SS5/SS13). **This is the most important decision in this entry**: the subscription/entitlement check (`hasActiveEntitlement()`) is hard-coded to always return `false`, because Subscription/Entitlement (Phase 4) does not exist in this codebase yet. This means contact access **cannot currently be granted to any seller, under any circumstances** — a deliberate fail-closed default, not a placeholder bug. The method and its comment explicitly say not to "temporarily" flip it to unblock testing; the e2e test in this pass asserts the denial, not a workaround. Every access attempt (granted or denied) is recorded in a new `ContactAccessGrant` table and the generic `AuditEvent` log.
- **Schema additions** (additive-only migration, `20260907012744_interests_contact_leads`, no drops/truncates): `SavedListing`, `Interest`, `Lead`, `LeadActivity`, `ContactAccessGrant`, `LeadStatus` enum. Reused the existing `Consent` model (`ConsentType.BUYER_CONTACT_SHARE`) for buyer consent rather than adding a new consent table.
- **Seed data extended:** `lead:moderate` permission (RISK_ANALYST + ADMIN) for the SPAM_FRAUD moderation action.
- **Verified:** `nest build` clean; 24/24 unit tests pass; 10/10 e2e tests pass (7 pre-existing + 3 new in `test/engagement.e2e-spec.ts`), including: interest/lead creation with self-interest and duplicate-interest rejection, lead status progression and terminal-state enforcement, RBAC positive/negative on SPAM_FRAUD moderation, and — the safety-critical case — contact access denied even when the buyer has explicitly granted `BUYER_CONTACT_SHARE` consent, with the denial reason and an audit row both asserted.
- **Reasoning:** Completes Phase 3 (Matching & Opportunity) per the implementation order. No financial, fee, or security-authorization behavior was invented; the contact-access gate is intentionally more restrictive than the future end state (fails closed instead of guessing at an entitlement check), consistent with CLAUDE.md SS16 ("stop if... confidential/authorization would be weakened") — the safe direction to err in is denial, not invented permissiveness.

## 2026-09-07 — Subscription Plan/Entitlement catalog, deliberately without activation
- **Decision point:** Before building Phase 4 (Subscription), asked the founder how to handle activation given no payment provider is chosen yet (Open Decision #1). Options were: (a) catalog only, no activation; (b) also add a manual admin-attested activation stand-in; (c) skip Subscription entirely for now. **Founder chose (a).**
- **Built:** `SubscriptionPlanService`/`SubscriptionPlanController` (`apps/api/src/modules/subscription/`) — admin-managed (`subscription:manage`) `SubscriptionPlan` + generic key/value `SubscriptionEntitlement`. Public read of active plans (buyers/sellers need to see pricing before subscribing); admin-only create/update/entitlement-upsert. Plan core terms (key, price, currency, billing period) are immutable after creation by design — changing them would retroactively affect anyone already on the plan; the pattern is to create a new plan and deactivate the old one, same as `MatchingProfile` versioning.
- **Explicitly not built:** `Subscription` (a seller's actual paid instance), `SubscriptionPayment`, and any activation flow. Building any of these — even a "manual admin marks it paid" stand-in — would mean establishing a real money-collection path before the payment-provider and legal/funds-structure decisions (Open Decision #1) are made, which CLAUDE.md's Stop Conditions (SS16) flag as founder-only territory. `ContactAccessService.hasActiveEntitlement()` therefore continues to always return `false`, unchanged from the previous entry.
- **Seed data:** `subscription:manage` permission (ADMIN only); illustrative starter catalog (FREE, WEEKLY_SELLER at ₦5,000/week, MONTHLY_SELLER at ₦15,000/month, matching the SSOT's introductory pricing) with example entitlement rows (`listing_limit`, `lead_contact_access`) — entitlement *meaning* is not enforced by any code yet, this is catalog data only.
- **Verified:** `nest build` clean; 24/24 unit tests pass; 12/12 e2e tests pass (10 pre-existing + 2 new in `test/subscription.e2e-spec.ts`), including RBAC positive/negative on plan/entitlement management, duplicate-key rejection, and active-vs-all listing visibility.
- **Reasoning:** Delivers the non-financial half of Phase 4 without pre-empting a decision that is explicitly reserved for the founder/legal per CLAUDE.md SS16 and SS39 ("do not invent... production payment/banking provider... final production fee collection mechanics").

## 2026-09-08 — Web frontend: from placeholder to a real, working app
- **Decision point:** After a full backend-only session (Phases 3-10 groundwork, all via Swagger/API only), asked the founder what "the next stage" meant. Options offered: build the web frontend, or make the payment-provider decision (Open Decision #1) to unblock Payments/Ledger. **Founder chose the web frontend** — no blocked decision needed for this.
- **Built:** Replaced the Phase-1 placeholder `apps/web` page with a working Next.js App Router app: `lib/api.ts` (typed fetch wrapper, no business logic — CLAUDE.md SS28 "never rely on frontend hiding for authorization"), `lib/auth-context.tsx` (React context, JWT stored in `localStorage`, hydrates `/users/me` on load), `lib/format.ts` (BigInt-safe money formatting — money always sent to the API as a raw integer, formatted string never round-tripped back into a request), `components/Header.tsx`/`RequireAuth.tsx`/`StatusBadge.tsx`, and full pages: home, register (captures an optional `?ref=` referral code), login, browse/search listings, listing detail + "I'm Interested", post-a-listing (dynamic category → subcategory → attribute form), My Listings (submit/mark-sold/renew), listing interests (seller view), buyer-request create (dynamic hard/preferred requirements) + list + matches, leads list/detail (seller status transitions, contact-access request — demonstrates the fail-closed denial honestly rather than hiding it, and the "Secure This Deal With TrustMart Escrow" handoff CTA), Escrow list/detail (accept/decline/cancel, versioned terms/conditions/fee-allocation display), and Account (referral code + copyable invite link).
- **Port change:** Founder flagged that `localhost:3000` is already used by another project ("Yinkexa") on this machine. Moved `apps/web` to **port 3001** everywhere: `package.json` dev script, `.claude/launch.json`, `.env.example`, and `apps/api/.env`'s `WEB_APP_ORIGIN` (for CORS).
- **Backend fixes found while building the frontend:** `BuyerRequestService.listMine()` and `ListingService.listMine()`/`listActive()` weren't including the `subcategory`/`category` relations, so the UI had no category label to show for "my buyer requests" and had to fall back to a raw ID. Added the includes — small, safe, read-only additions with no behavior change beyond returning more of what was already there.
- **Demo data:** Seeded two real starter categories (Real Estate → Duplex/Apartment, Vehicles → Sedan) with attributes (bedrooms, land size, year, mileage), matching the SSOT's rollout order, so the app has real browsable data without requiring manual admin setup first.
- **Verified live, not just compiled**: registered a real user through the UI, browsed real listings, posted a new listing through the dynamic form (category → subcategory → Year/Mileage attributes → price → submit), watched it flow DRAFT → SUBMITTED via the "My Listings" action button, expressed interest on another listing (created a real Interest + Lead server-side), created and activated a buyer request with a dynamic HARD requirement, and confirmed the Account page shows the real referral code and correct Company Referral attribution. Backend suite re-run after the two `listMine`/`listActive` fixes: `nest build` clean, 24/24 unit tests, 28/28 e2e tests, all still green.
- **Pending:** Founder is saving the official TrustMart shield/icon logo to a file; wiring it in as the favicon and header logo is the very next step once a file path is provided (currently text-only "TRUSTMART" wordmark in the header, matching the brand colors but not yet the shield mark).
- **Reasoning:** This is the first user-facing surface for everything built in this session — every screen calls the real API with no mocked data, so bugs the API's own e2e suite couldn't catch (like the missing category includes) surfaced immediately.

## 2026-09-08 — Restyled the web app to match the founder's brand identity and dashboard mockups
- **Context:** Founder pointed to `C:\Projects\TrustMart\Logo and brand Identity\` — the official shield/icon logo, wordmark, monochrome variant, and 8 dashboard mockup screens (Client Dashboard, Marketplace home, Create Escrow wizard, Escrow Details, Payments, Marketplace Matches, Dashboard, Wallet) — and asked for the web app to match this look: logo, fonts, design, and dashboard pattern, not just the navy/gold color tokens already in place.
- **Built:** Replaced the plain top-nav `Header` with a `Sidebar` (dark navy, logo lockup, gold "+ Post a Listing" CTA, grouped nav — Dashboard / Marketplace (Browse Listings, My Requests, My Listings, Leads) / Escrow (My Escrows) / Account — active-state highlighting, bottom security-reminder card + logout) and a `Topbar` (search bar, user menu / login-signup) on every page, composed once in `app/layout.tsx` so every route gets it automatically. New `/dashboard` page as the landing page after login, with real aggregate stat cards (listings/buyer-requests/escrows/referrals counts pulled from the actual `*/mine` endpoints — no fabricated numbers, no fake trend arrows since there's no historical snapshot data to compute them honestly from). Escrow detail page gained a 4-step visual tracker (Created → Terms Accepted → Funding → Completed) mapped from the real `EscrowStatus` enum, with an honest note at the Funding step explaining it's pending a payment-provider decision rather than hiding the gap. Buyer-request Matches page gained score progress bars and classification labels matching the mockup's match-card style. Added `components/PasswordInput.tsx` (show/hide toggle) to login/register per a follow-up founder request.
- **Logo integration:** Copied the founder-supplied `TrustMart App Logo.png` into `apps/web/public/tm-icon.png` and `apps/web/app/icon.png` (Next.js App Router's automatic favicon convention — no manual `<link>` tag needed). Used in the Sidebar header, Topbar (mobile/logged-out), and the Login/Register pages.
- **Bug found and fixed:** the Sidebar's active-nav-link check used `pathname.startsWith(href)`, so visiting `/listings/mine` incorrectly highlighted both "My Listings" and "Browse Listings" (since `/listings/mine` starts with `/listings`). Fixed with a longest-matching-prefix rule (`isActiveHref()`) so only the most specific nav item lights up — caught by actually looking at the rendered sidebar, not by code review alone.
- **Dev-data hygiene (non-destructive):** the dev database had accumulated ~87 junk categories and ~104 junk listings from every e2e test run across the session (titles like "Spam test listing", categories like "Engagement Subcategory"), which cluttered the now-visible Marketplace UI. Deactivated the categories (`isActive: false`) and archived the listings (`status: ARCHIVED`) via two one-off scripts — status flips, not deletes, fully reversible, and scoped to exact known test titles/keys so nothing real was touched. Added a permanent demo seller (`demo.seller@trustmart.ng`) and two realistic ACTIVE listings to `seed.ts` so the Marketplace always has presentable content out of the box, instead of relying on leftover test data.
- **Verified:** `next build` production build is clean (18 routes, all typecheck and prerender); backend unchanged (24/24 unit, e2e suite unaffected — this session touched no API contracts, only the seed script and frontend). Most importantly, exercised a **complete live Escrow lifecycle end-to-end through the real UI/API**, not just unit-level: registered/used two real accounts, buyer expressed interest on a seeded listing, seller ran the Marketplace-to-Escrow handoff from the resulting Lead, buyer accepted the terms, and the step tracker was screenshotted correctly advancing DRAFT/TERMS_PROPOSED ("Terms Accepted", current) → ACCEPTED ("Funding", current, with the pending-payment-provider note) in the browser.
- **Reasoning:** A functionally-correct app that doesn't look like the approved brand isn't done — the founder's mockups are the actual design spec, and matching them (not inventing a different layout) is what "tailor to match the look" means here.

## 2026-09-08 — Mobile nav bug and a second live-testing-only Escrow bug
- **Context:** Founder asked to keep going on polish after the brand-identity restyle. Added a mobile drawer for the sidebar (real bug, not just missing polish: the sidebar was `hidden lg:flex` with literally no way to open it below `lg`, so mobile users had zero navigation), then hit a second real bug while testing it.
- **Bug 1 — mobile nav unreachable:** Added `lib/mobile-nav-context.tsx` and a hamburger button in `Topbar` (visible `lg:hidden`) that opens a slide-over drawer sharing the same `SidebarContent` component as desktop, auto-closing on route change. Verified at a 375px viewport.
- **Bug 2 — `EscrowService.listMine()` missing `termVersions`:** While testing the mobile dashboard with a real escrow present (created via the founder's own earlier "keep going" testing flow), the Dashboard page went completely blank. Root cause: `GET /escrows/mine` only ever included `parties`, never `termVersions` — the Dashboard and Escrows-list pages both read `escrow.termVersions[0].transactionAmountMinorUnits` to show the amount, copying that shape from `GET /escrows/:id` (which does include it). This was invisible the whole time because the demo account had zero escrows until this session's own live testing created one. Fixed by adding the same `termVersions` include (with `conditions`/`acceptances`) already used in `get()`; hardened both frontend read-sites with optional chaining as defense-in-depth; added a regression e2e test in `escrow.e2e-spec.ts` that specifically asserts on `GET /escrows/mine`'s shape, since every prior escrow test exercised `GET /escrows/:id` instead.
- **Process note:** Also hit two rounds of Next.js dev-server/tab tooling noise unrelated to the app itself — a long-lived browser tab accumulated Fast-Refresh corruption after many hot-reloads across a provider-tree structural change (fixed by testing in a fresh tab), and running `next build` while `next dev` was still running against the same `.next` directory corrupted the dev server's module cache (fixed by stopping the dev server, deleting `.next`, and restarting). Neither was a code bug; recorded here only so a future session recognizes the same symptoms quickly.
- **Verified:** `nest build` clean; 24/24 unit tests pass; 29/29 e2e tests pass (28 pre-existing + 1 new regression test); `next build` clean (18 routes). Confirmed live: mobile drawer opens with full nav content at 375px, and the Dashboard/Escrows pages correctly show the real escrow amount and status after the fix.
- **Reasoning:** Both bugs were found by actually using the app end-to-end at the exact moment enough real data existed to expose them — underscoring why "verified end-to-end in-browser," not just "unit tests pass," is the bar for calling a UI feature done.

## 2026-09-08 — Home page rebuilt to match founder mockup; declined to copy its fabricated stats
- **Context:** Founder asked for the home page to match `C:\Projects\TrustMart\Logo and brand Identity\Home page design.jpg` exactly, with the Hero section content used verbatim.
- **Built:** Rebuilt `apps/web/app/page.tsx` to match the mockup's structure and copy: navy gradient hero (exact headline/subtext/CTA copy and trust-icon row), stats strip, "Simple. Safe. Effective." 4-step section, "Explore Top Categories" grid, closing Marketplace/Escrow CTA. Added several icons (`LockIcon`, `HeadsetIcon`, `BoxIcon`, `StarIcon`, `CheckCircleIcon`, `ArrowRightIcon`) to the existing inline icon set to support it.
- **Deliberate deviation — stats bar:** The mockup's stats bar shows "50,000+ Active Users", "25,000+ Listings", "₦2B+ Transactions Protected", "4.8/5 User Satisfaction". TrustMart has none of that yet — it's a pre-launch platform with a handful of demo listings. Copying those figures verbatim would be publishing fabricated numbers to real visitors. Replaced with real, live figures instead (active listing count and category count fetched client-side from the public `GET /listings` / `GET /categories` endpoints) plus an honest "Optional" Escrow-protection stat. The founder's instruction to match the design applied explicitly to the Hero section's content; the stats bar is not the Hero, and no instruction overrides the general house rule (already established for the Dashboard) against inventing marketing numbers.
- **Deliberate deviation — hero photography:** The mockup's hero includes a phone-in-hand product photo with two models. No such photo asset exists in the brand folder, and fabricating stock photography of people to depict real customers would misrepresent the product. Substituted a card/icon illustration built from the existing shield logo and the same speech-bubble/lock-badge concepts shown in the mockup ("Find it. Buy it." / "List it. Sell it." / "Secure it. With TrustMart Escrow.") — same idea, real assets only.
- **Side effect — second category cleanup:** Rendering the new "Explore Top Categories" grid surfaced 13 leftover e2e test-fixture categories (mirroring the listings pollution found in the previous session) alongside the 2 real demo categories. Deactivated the 13 via a one-off, non-destructive `isActive: false` script; kept `real-estate` and `vehicles`.
- **Verified:** `next build` clean; confirmed live in-browser at desktop and 375px mobile widths — hero copy, stats, how-it-works, and categories grid all render correctly with real data.
- **Reasoning:** "Match the mockup" and "never publish fabricated numbers/photos" are both standing rules; where they'd conflict on decorative content not covered by the explicit Hero-content instruction, honesty wins without needing to ask, per CLAUDE.md's Financial Safety and MVP-discipline spirit — but this is recorded here rather than silently decided, since it is a visible deviation from a founder-supplied design file.

## 2026-09-08 — Isolated e2e tests into a dedicated `trustmart_test` database
- **Context:** Rendering the redesigned home page's "Explore Top Categories" grid surfaced test-fixture pollution for the *second* time this project (13 junk categories, on top of the 18 junk listings found the same day). Both prior cleanups were one-off manual scripts against `trustmart_dev` — they fixed the symptom but not the cause: every `pnpm test:e2e` run writes ~10-20 rows into whatever database `DATABASE_URL` points at, and that has always been the same `trustmart_dev` database the web app demos against. Manually re-cleaning after every test run is not sustainable and was going to keep recurring.
- **Built:** Created a second database, `trustmart_test`, on the same portable PostgreSQL instance (port 5433) — no new infrastructure, just a second schema. Added `apps/api/.env.test` (gitignored, same pattern as `.env`) pointing at it, applied all 5 existing migrations and the seed script to it. Added `apps/api/test/jest-e2e.setup.ts`, wired into `jest-e2e.json` via `setupFiles`, which loads `.env.test` into `process.env` before any test file or Nest module runs — hand-rolled parsing rather than the `dotenv` package, since pnpm's strict `node_modules` only exposes `dotenv` as a transitive dependency of `@nestjs/config`, not something `apps/api` can `require` directly without adding it as a real dependency for a two-line need.
- **Verified:** Ran `pnpm test:e2e` — all 9 suites / 29 tests pass unchanged against `trustmart_test`. Confirmed via direct `psql` counts that `trustmart_dev`'s row counts did not move during the run while `trustmart_test` absorbed the new rows. Cleaned the now-stable `trustmart_dev` of the 18 previously-accumulated test-fixture listings (same non-destructive `ARCHIVED` status flip as before) and confirmed live in-browser that `/listings` now shows only the 2 real demo listings.
- **Reasoning:** This directly serves CLAUDE.md SS40 ("Testing Requirements") without compromising SS38 (MVP scope discipline) — it's dev/test tooling hygiene, not a product feature, and required no schema, business-rule, or API change. Doing it now (rather than re-cleaning a third time) stops the same manual cleanup from recurring after every future test run.

## 2026-09-08 — Admin Control Centre UI, built against Phase 10's existing endpoints
- **Context:** Phase 10 (`59fd997`, an earlier commit) added `GET /admin/analytics/overview` and `GET /admin/audit-events` — RBAC-gated, tested, working — but no web UI ever called them. Continuing the standing "keep going" instruction, closed that gap rather than starting a new backend feature, since CLAUDE.md SS25/SS35 lists Admin analytics/audit as an expected dashboard area and the backend work was already done and idle.
- **Built:** `/admin/analytics` and `/admin/audit` pages in `apps/web`, plus a `RequirePermission` client-side gate (mirrors the existing `RequireAuth` pattern) and a Sidebar "Admin" section that only renders for accounts with the relevant permission.
- **One small, deliberate backend addition:** `GET /users/me` now also returns `permissions: string[]` (`UsersService.getSelf`, via the existing `RbacService.getPermissionKeysForUser` — no new query logic, just reuse), so the frontend can decide whether to show the Admin nav at all instead of showing a link that 403s for everyone but admins. Explicitly commented in both `users.service.ts` and `RequirePermission.tsx` as a UX convenience only — every admin endpoint still independently enforces its own `PermissionGuard` server-side (CLAUDE.md SS32), and this was verified, not assumed: a non-admin account was confirmed to get a clean "Access restricted" message from the frontend gate *and* a real 403 from the API if it bypasses the frontend and hits the endpoint directly.
- **Also added:** a `demo.admin@trustmart.ng` seed account (ADMIN role) — there was previously no way to exercise the Admin Control Centre without manually granting a role via Prisma, which isn't a realistic test path for future sessions or the founder.
- **Verified:** `next build` clean (20 routes, up from 18); all 30 e2e tests pass (29 existing + 1 new regression on `/users/me`'s `permissions` field) against the isolated `trustmart_test` database; 24/24 unit tests pass; confirmed live in-browser as both the demo admin (sees Admin nav, both pages render real aggregate data) and the demo seller (no Admin nav entry, "Access restricted" on direct navigation).
- **Reasoning:** MVP scope discipline (CLAUDE.md SS38) argues against inventing new backend surface area, but this wasn't new surface area — it was finishing a phase that had a dangling, untested-from-the-UI backend half. No financial figures are shown (the analytics page says so explicitly), no write endpoints were touched, and the only backend change is additive and re-uses existing RBAC logic.

## 2026-09-08 — Adversarial security review, second pass (CLAUDE.md SS45)
- **Context:** The first review pass (2026-09-07) covered commits up through the initial Marketplace/Escrow/Swagger work. Eight more commits landed since then — mobile nav, an Escrow `listMine()` bug fix, the home page/hero rebuild, e2e test-database isolation, and the new Admin Control Centre UI (which added a new `permissions` field to `GET /users/me` and two new RBAC-gated pages) — none of which had been reviewed. Ran the `security-review` skill's two-phase process (identify → false-positive-filter, each as a dedicated sub-agent) rather than skip straight to "looks fine," per SS45.
- **Findings:** Zero reportable. The identification pass surfaced one candidate — the hardcoded `demo.admin@trustmart.ng` / `Demo12345!` seed credential added for testing the new Admin UI — at self-rated confidence 7/10. The filtering pass verified there's no environment guard around it, but also confirmed no deployed/staging/production environment exists yet for this repository (`OPEN_DECISIONS.md` still lists "first deployed environment" as unresolved) and the credential is already commented as dev-only in the source; scored it 3/10, below the reporting threshold — a real future hardening item, not a concrete vulnerability in the repo's current state.
- **Also confirmed (not new findings, but worth recording as verified rather than assumed):** every resource-owning controller/service touched or added since the last review (Escrow, Listing, Engagement, Matching, Subscription, Referral, Category, BuyerRequest, Business, Consent, Verification, PlatformConfig, Identity) still does an ownership/party check before any read or write; every admin route stacks `PermissionGuard` server-side; the new frontend `RequirePermission` gate is explicitly documented and confirmed as UX-only (a non-admin was verified to still get a real 403 from the API directly, not just a client-side redirect); no `passwordHash` reaches any audit-log call site; no `dangerouslySetInnerHTML` anywhere in `apps/web`.
- **Reasoning:** CLAUDE.md SS45 requires an adversarial review after sensitive phases, and RBAC-gated admin surface area plus a new field on the primary identity endpoint both qualify. Running it now (rather than waiting for a bigger backlog) keeps the review scoped to a reviewable diff instead of a multi-week accumulation.

## 2026-09-07 — Listing expiry/renewal sweep
- **Built:** `ListingLifecycleService` (`apps/api/src/modules/listing/listing-lifecycle.service.ts`) — `sweep()` moves ACTIVE listings to EXPIRING inside a new configurable warning window (`marketplace.listing_expiry_warning_days`, default 2 days) and EXPIRING/ACTIVE listings past `expiresAt` to EXPIRED; idempotent, safe to call repeatedly. `renew()` is the seller's explicit "STILL AVAILABLE" response (CLAUDE.md SS10), resetting an EXPIRING/EXPIRED listing to ACTIVE with a fresh `expiresAt`. `ListingService.markSold()` extended to also accept EXPIRING, since a seller responding "SOLD" to the same prompt uses the existing mark-sold endpoint.
- **Endpoint design:** `POST /listings/lifecycle/sweep` is gated on a new `listing:lifecycle_sweep` permission (ADMIN) rather than being an internal cron — this repo has no scheduler dependency installed, and CLAUDE.md SS26 already establishes that n8n may call authenticated TrustMart APIs on a schedule for exactly this kind of periodic trigger, while the backend keeps sole authority over the actual state transition. No new npm dependency was added.
- **Verified:** `nest build` clean; 24/24 unit tests pass; 15/15 e2e tests pass (12 pre-existing + 3 new in `test/listing-lifecycle.e2e-spec.ts`), covering RBAC on the sweep endpoint, the full ACTIVE→EXPIRING→EXPIRED→renew cycle (using direct Prisma manipulation of `expiresAt` to avoid real-time waits), rejection of renewal by a non-owner and of an already-ACTIVE listing, and marking SOLD from EXPIRING.
- **Deferred:** price history already existed from Phase 2; re-engagement notifications (re-matching/notifying on price drops, expiry reminders) are n8n/notification territory (Phase 11) and not part of this pass.
- **Reasoning:** Completes the listing side of Phase 4's "listing expiry/renewal" item without inventing scheduling infrastructure or touching anything financial.

## 2026-09-07 — Standalone Escrow, non-financial scaffolding only
- **Decision point:** Before starting Phase 5 (Escrow), flagged it explicitly rather than proceeding on the same "continue autonomously" basis as earlier phases — Escrow concentrates the large majority of CLAUDE.md's own Stop Conditions (legal funds structure, provider selection, fee/reward conflicts, ledger mutation, financial DB bypass). Asked the founder how to scope it; **founder chose non-financial scaffolding only**, over pausing Escrow entirely or a different sequencing.
- **Built:** `EscrowService`/`EscrowController` (`apps/api/src/modules/escrow/`) — one Escrow Engine regardless of origin (`EscrowOriginType`: DIRECT/MARKETPLACE/PARTNER/BUSINESS_API; `originListingId` is optional/nullable context only, not validated against the Listing table, per CLAUDE.md SS3's "Marketplace may feed Escrow, but Escrow must never depend on Marketplace"). `create()` invites parties and proposes version-1 terms atomically; `proposeAmendment()` creates a new term version, resets status to TERMS_PROPOSED, and requires every party to re-accept (CLAUDE.md SS14: "No party may secretly alter accepted terms. A material amendment creates a new version requiring required acceptance again."); `accept()` only transitions DRAFT/TERMS_PROPOSED to ACCEPTED once every current party has accepted the *active* term version; `decline()` (only pre-acceptance) and `cancel()` (any non-cancelled state, reason required) both move to CANCELLED. `feePercent` is snapshotted onto each `EscrowTermVersion` from `PlatformConfiguration` at proposal time, not read live later, so a future config change can't retroactively alter an already-agreed version.
- **Explicitly not built:** anything past ACCEPTED — no FUNDING_INSTRUCTIONS/VERIFIED_FUNDING/ACTIVE/FULFILMENT/RELEASE/COMPLETION states, no KYC/risk gating, no payment provider integration, no ledger. `EscrowStatus` in the schema only has the four pre-funding values, with a comment explaining why. This mirrors the same reasoning as the Subscription-catalog decision earlier today: don't invent a money-handling flow ahead of Open Decision #1 and the legal customer-funds-structure decision, both explicitly reserved for the founder/legal per CLAUDE.md SS16/SS39/SS46.
- **Schema additions** (additive-only migration, `20260907201755_escrow_scaffolding`, no drops/truncates): `EscrowTransaction`, `EscrowParty`, `EscrowTermVersion`, `EscrowCondition`, `EscrowAcceptance`, plus `EscrowOriginType`/`EscrowStatus`/`EscrowPartyRole`/`EscrowPartyStatus`/`EscrowFeeAllocation` enums. Reuses the generic `AuditEvent` log instead of a dedicated `EscrowEvent` table (same simplification pattern as listing/buyer-request lifecycle events).
- **Verified:** `nest build` clean; 24/24 unit tests pass; 19/19 e2e tests pass (15 pre-existing + 4 new in `test/escrow.e2e-spec.ts`), covering creation with versioned terms, mutual-acceptance gating, a stranger being rejected from reading or accepting, a material amendment bumping the version and resetting acceptance (requiring the other party to re-accept before the new terms take effect), decline-before-acceptance, and cancel-with-reason (including rejecting a double-cancel).
- **Reasoning:** Delivers the party/terms/acceptance/conditions/fee-allocation-as-a-term parts of Phase 5 that don't require a funds-flow decision, while leaving the actually money-adjacent parts explicitly for Phase 6/7 once Open Decision #1 is resolved.

## 2026-09-07 — Company Referral (relationship tracking only, no reward payout)
- **Built:** `ReferralService`/`ReferralController`/`ReferralModule` (`apps/api/src/modules/referral/`). Every new user gets a unique 8-character referral code (generated via `node:crypto` `randomBytes`, alphabet excludes 0/O/1/I to avoid transcription errors) at registration. `AuthService.register()` now calls `ReferralService.assignReferralOnRegistration()` after the identity is created, regardless of which `AuthProvider` backs it — referral tracking is a TrustMart concern, not an identity-provider concern. If a valid `referralCode` was supplied in the register payload, `ReferralRelationship.referrerType = USER` with the referrer linked; otherwise (missing or unknown code) it's `COMPANY` — never left null, per CLAUDE.md SS19. An unknown/invalid code is a soft fallback, never a registration-blocking validation error.
- **Abuse vectors:** CLAUDE.md SS19 calls out detecting self-referral and circular referral. Both are structurally impossible in this data model rather than needing a runtime check: a brand-new user has no `referralCode` yet at the exact moment they'd need one to refer themselves, and a `ReferralRelationship` is created exactly once at registration and never edited afterward, so no cycle can ever form (the referral graph is a forest ordered strictly by registration time). Fake-account/fabricated-transaction/collusive-reward-farming detection is explicitly TrustGuard territory (Phase 7+) and is not attempted here.
- **Explicitly not built:** reward computation or payout of any kind. The confidential 0.3%/0.1%/0.1% policy (CLAUDE.md SS18) requires a completed, funded Escrow transaction to trigger against — that requires Phase 6 (Payments & Ledger), which is blocked on Open Decision #1. This entry only tracks *who referred whom*; no reward amount, `RewardPolicyVersion`, or `RewardWallet` exists yet, and none of this code path ever touches money.
- **Schema additions** (additive-only migration, `20260907224500_company_referral`, no drops/truncates): `User.referralCode` (nullable, unique), `ReferralRelationship` model, `ReferralType` enum. Applied via `prisma migrate diff` (DB-to-schema) + `prisma migrate deploy` instead of `migrate dev`, because `migrate dev` requires an interactive TTY to confirm the (harmless, since existing rows all have `NULL` and Postgres allows multiple `NULL`s in a unique column) new-unique-constraint warning, which this non-interactive shell can't provide. Generated SQL was inspected before applying — additive only.
- **Verified:** `nest build` clean; 24/24 unit tests pass; 25/25 e2e tests pass (21 pre-existing + 4 new in `test/referral.e2e-spec.ts`), covering unique-code assignment, Company Referral default, valid-code USER attribution (including the referrer's count updating), invalid-code fallback to Company Referral, and auth-required on the read endpoint.
- **Reasoning:** Delivers the one part of Phase 8 (Referral/Rewards/Reviews) that doesn't require completed/funded Escrow transactions to exist first, without touching money, ledger, or the confidential reward policy in any way.

## 2026-09-07 — Admin analytics overview + audit-event browsing (Phase 10 groundwork)
- **Built:** `AnalyticsService`/`AnalyticsController` (`GET /admin/analytics/overview`, new `analytics:read` permission, ADMIN only) — read-only aggregate counts (users, listings/buyer-requests/leads/escrows by status, match totals and qualification rate, interests, active subscription plans, referrals by type) via `Promise.all` + Prisma `count`/`groupBy`. `AuditController` (`GET /admin/audit-events`, gated on the existing `audit:read` permission) — filterable by `resourceType`/`action`, paginated — added to `AuditModule` alongside a new `AuditService.listRecent()` method; the module was already `@Global` for provider access but had no HTTP surface until now.
- **Scope:** Deliberately just counts/group-bys, not the full Marketplace funnel (CLAUDE.md SS35) or Liquidity Intelligence (SS36), which need event-timestamp sequencing and geography/demand-supply breakdowns not built yet. No financial figures (GMV, subscription revenue) since nothing has a completed/paid state yet — reporting a "revenue" number would be fabricating data.
- **Verified:** `nest build` clean; 24/24 unit tests pass; 28/28 e2e tests pass (25 pre-existing + 3 new in `test/admin.e2e-spec.ts`), covering RBAC positive/negative on both endpoints, unauthenticated rejection, and audit-event filtering/pagination correctness. Confirmed live in the Swagger docs preview (both new tagged groups render and are queryable).
- **Reasoning:** First piece of Phase 10 (Admin & Analytics) — read-only, no state mutation, no financial exposure, safe to build without founder input.
